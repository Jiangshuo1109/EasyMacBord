# EasyMacBord 桌面端需求总结

**版本：** v0.1 UI 改造，T03 可配置工具与本机应用；T04 桌面体验与动作中心（已实现，待完整 UI 与实物验收）
**载体：** macOS 26+ 独立菜单栏应用
**设计取向：** 面向已接入硬件的桌面配置工具。信息优先级清楚，使用系统控件和浅灰底色；墨绿只用于当前选择、同步命令和已确认状态。

## 用户与场景

目标用户是拥有 EasyInput V2.0 的 Apple Silicon Mac 用户。常见操作是：在工作开始前选择或新建配置档，为 8 个按键和单枚旋钮的三个事件设置动作，登记本机动作并同步到设备；同步后，在状态总览确认设备、配置、权限和可由应用确认的动作结果。

界面中的设备名称、连接状态、同步回执和权限状态必须来自运行时数据。设计稿中的文字仅表达字段和版式，不作为真机数据或固件能力的证明。

## 信息架构与窗口约束

- 主窗口默认 `1280 x 800`，最小 `1120 x 720`；低于最小尺寸不继续压缩为窄版布局。
- 左侧主导航为 `状态总览`、`配置档`、`动作库`、`设备与同步`、`权限`；底部单列 `设置` 和 `关于`。动作库是独立维护入口，配置档仍负责把动作或输入预设应用给特定控制件。
- 左侧顶部显示 EasyMacBord 图标与名称，并在下方独立显示运行时设备名称和连接状态；无连接时使用“未连接”，不展示推测的设备能力。
- 当前配置档显示在侧栏和页面标题附近；所有页面均可使用“同步当前配置”命令，但未建立可写入通道时不可执行。

## 主要页面与侧栏辅助项

### 状态总览

- 以设备、当前配置档、同步、权限四项摘要作为首屏信息。
- 展示 8 个按键与“旋钮左转、旋钮按下、旋钮右转”的映射预览；长名称单行截断，悬停显示完整名称。
- “本机动作记录”只记录应用实际尝试并得出结果的 Host Action，以及用户点击测试产生的结果。固定文本和键盘组合键没有应用侧可确认的执行回执，不写入该记录。

### 配置档

- 使用主侧栏、编辑区、设置检查器三栏结构。编辑区顶部选择配置档并提供“新建配置档”；中栏以 `4 x 2` 固定键帽和单枚旋钮表现 11 个事件；右栏编辑当前选中的控制件。
- 设置检查器用分段控件选择动作类型：本机动作、固定文本、键盘组合键、禁用。本机动作和输入预设均从动作库选择；不在检查器内重复维护动作目录。
- 键帽和旋钮选择区尺寸固定，长动作名称截断后可查看完整值。已登记应用在映射预览中显示运行时解析的图标。
- 直接工具包括系统截图与录屏界面、锁定屏幕、仅防止闲置显示器休眠、用户选图壁纸、默认输出音量、隐藏前台应用和 Apple Music 的播放/暂停、上一首、下一首。它们与应用目标一样，先在动作库登记再映射。
- 深色模式、启动屏保、键盘清洁、隐藏桌面文件、隐藏 Dock、分屏布局、显示器亮度、键盘亮度显示为快捷指令模板。用户需提供自己的快捷指令名称和权限，应用不创建快捷指令或使用私有系统接口。
- 不显示动作 UUID、安全书签、绝对路径或其存储位置。设备只接收 UUID 的事实以简短说明呈现，不泄露具体值。
- 测试按钮只对已登记的本机动作可用。固定文本和键盘组合键只能通过实体设备测试；“锁定屏幕”一类影响当前会话的系统动作在测试前必须二次确认。

### 动作库

- 集中维护已登记的应用、网址、快捷指令和直接系统工具，也维护固定文本与键盘组合键预设。用户可以搜索、筛选、登记、编辑、删除或选择条目。
- 应用候选来自用户、本地和系统应用程序目录，只显示名称、图标和来源；用户确认后才保存安全书签。图标不写入持久化数据或设备配置。
- 固定文本和组合键预设是本机数据。选择时将标题和值复制给控制件；后续编辑预设不会反向修改控制件。组合键必须在录制控件中实际按下，不能靠自由文本录入。

### 设备与同步

- 只展示连接状态、当前配置档、当前同步状态、上次确认时间和回执摘要。
- 固件版本、备用通道和设备能力在没有状态读取协议时统一显示“未读取”，不得以配置通道存在推断这些信息。
- 蓝牙相关表述统一为“蓝牙配置通道”。USB 可写时优先使用 USB；USB 写入失败不自动改走蓝牙配置通道。
- 本次同步失败时，页面保留上一次已确认的时间和回执摘要，并单独显示本次失败原因。

### 权限

- 展示辅助功能和自动化。辅助功能需要用户明确发起系统授权请求，并在应用重新激活或用户重新检查时刷新；自动化在首次相关执行后显示系统结果。
- 当前“截图与录屏”只打开系统截图面板，不直接录制或读取屏幕内容，因此不展示屏幕录制权限。只提供明确命令，不在应用内模拟授权成功。

### 设置与关于

- 设置提供菜单栏显示与开机启动。菜单栏默认开启；开机启动默认关闭，只有用户切换时才调用登录项服务，并显示系统返回状态。
- 关于显示应用图标、版本、构建号、架构和内部 ad-hoc 测试包说明。

## 本版边界

- 不增加固件状态读取、设备能力探测或新的设备协议字段。
- 不修改 Maker 固件、硬件、烧录、设备身份、S3C v1 格式或 USB 优先规则。
- 不提供移动端布局、云同步、AI、语音输入、Wi-Fi 音频、音乐律动和 DDC。
- 不内置深色模式、屏保、键盘清洁、隐藏桌面文件/Dock、分屏、显示器亮度或键盘亮度的系统设置逻辑；这些能力只经用户快捷指令接入。

## 设计交付物

- `主界面.html`：状态总览正常态及窗口、侧栏和摘要的版式参考。
- `全状态设计参考.html`：配置档三栏结构、设备与同步、权限以及加载、空、同步失败等状态参考。
- `交互规则.md`：操作、数据边界和反馈规则。
- `UI改造实施方案.md`：页面职责、实现顺序和截图验收条件。
- `T04桌面体验与动作中心设计说明.md`：Dock 窗口恢复、动作库、输入预设、宏键盘、设置和权限的 T04 设计边界。
