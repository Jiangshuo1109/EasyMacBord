# T04 软件验收证据

此目录只保存 T04 桌面端人工验收的脱敏截图或简短结果记录。已有一张发布包默认窗口观察截图，但它不表示 T04 UI 验收已经完成。

## 可保存的证据

| 编号 | 状态或场景 | 预期文件名 |
|---|---|---|
| UI-01 | Dock 恢复主窗口 | `ui-01-dock-reopen-1280x800.png` |
| UI-02 | 品牌与页面标题 | `ui-02-brand-title-1280x800.png` |
| UI-03 | 宏键盘与旋钮映射 | `ui-03-macropad-1280x800.png` |
| UI-04 | 动作库与应用图标 | `ui-04-action-library-1120x720.png` |
| UI-05 | 文本预设与组合键录制 | `ui-05-input-presets-1120x720.png` |
| UI-06 | 设置、关于与权限 | `ui-06-preferences-permissions-1120x720.png` |

## 当前已保留观察

- `overview-1280x800.png`：已查看发布包默认 `1280 x 800` 窗口的状态总览。动作库、宏键盘、设置和关于也在同一次发布包人工查看中打开，但没有分别保存截图。
- 这项观察不将 UI-01 至 UI-06 标记为通过；尤其不替代 `1120 x 720` 的 Xcode UI 测试或可信截图验收。

截图前使用 Debug 状态或专用测试数据，不能连接真实设备、读取真实用户路径或将测试结果写成硬件结论。不得保存 UUID、安全书签、绝对路径、设备唯一标识、用户固定文本、原始权限诊断或操作系统账户信息。

本目录只证明布局与可见状态。M-01 至 M-08 的本机工具结果记录在测试计划对应的人工记录中；HW-01 至 HW-07 的设备结论只记录在 `flow/integration-record.md`。
