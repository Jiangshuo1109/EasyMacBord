# T02 UI 截图证据

截图只来自 Debug 构建，使用 `--ui-state` 固定状态，不接入设备，也不读取本机配置、动作或权限数据。

当前状态：2026-08-26 尝试通过本机自动化界面服务读取运行窗口时连续超时，尚未生成可审计截图。以下项目全部保持待验收，不能视为通过。

| 窗口尺寸 | 页面与状态 | 文件 |
|---|---|---|
| 1280 x 800 | 状态总览，`sync-confirmed` | 待生成 |
| 1280 x 800 | 配置档，`empty-actions` | 待生成 |
| 1280 x 800 | 设备与同步，`sync-failed` | 待生成 |
| 1280 x 800 | 权限，`permission-denied` | 待生成 |
| 1120 x 720 | 状态总览，`long-names` | 待生成 |
| 1120 x 720 | 配置档，`disconnected` | 待生成 |
| 1120 x 720 | 设备与同步，`sync-sending` | 待生成 |
| 1120 x 720 | 权限，`connecting` | 待生成 |

验收重点：四项导航、文字截断、配置检查器、底部操作区、同步历史和权限操作均不重叠；界面不出现 UUID、绝对路径、安全书签或虚构设备信息。
