# 项目进展

## 2026-08-26 · 初始化 macOS 消费端

- **完成：** 建立 SwiftPM 工程、协议编解码、版本化配置档和本机动作书签存储、USB/BLE 路由代码、菜单栏和状态总览界面；补齐 SRS、原型和联调文档。BLE 配置已按逐帧 `.withResponse` 回调发送，保存确认增加分片头、长度和 CRC 校验，8 秒无确认记为失败。
- **证据：** `Sources/EasyMacBord/`、`Tests/EasyMacBordTests/`、`docs/`、`设计/v0.1-EasyMacBord桌面端/`。
- **测试：** `swiftc -parse`、发布脚本语法、plist 格式检查通过；排除 SwiftUI 层后的核心模块已使用 `swiftc -typecheck -target arm64-apple-macos26.0` 通过。`swift test` 已进入目标编译，但当前 Command Line Tools 缺少 `SwiftUIMacros` 插件，所有使用 `@State` 的 SwiftUI 文件因此不能完成编译。未报告任何单元测试通过。
- **未验证：** 完整 Xcode 类型检查与测试、USB/BLE 设备发现、配置保存回执、Host Action 执行、ad-hoc 签名、DMG、远程推送。
- **下一步：** 安装完整 Xcode 后复跑测试；接入兼容设备，按 `docs/测试/测试计划.md` 执行真机矩阵。

## 2026-08-26 · 本地版本基线

- **完成：** Git 忽略项、暂存差异和发布脚本已审计；本地 `main` 已创建初始提交。
- **远程：** 已配置 `origin` 为用户提供的 GitHub 仓库。两次非交互推送未返回成功结果，且未建立 `origin/main` 跟踪引用；不将远程同步记为完成。
- **下一步：** 在终端完成该 GitHub 账户的凭据配置后，执行 `git push -u origin main` 并核对远程提交号。
