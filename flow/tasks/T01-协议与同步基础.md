# T01 协议与同步基础

## 目标

实现并测试 S3C v1、配置确认和 Host Action 解码，不接入真机，不修改固件。

## 验收

- CRC 标准向量和所有长度边界具备测试。
- `0x03`、`0x04`、`0x05` 明确分流。
- UUID 非规范格式 fail closed。
- USB 优先、USB 失败不回退的路由有测试。

## 当前状态

代码和测试已在 Xcode 27.0 beta 6 下完成完整 Swift 测试。配置写入、保存确认和实体输入仍须以兼容设备的实际观察为准，见 `flow/integration-record.md`。
