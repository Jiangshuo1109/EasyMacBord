# 联调记录

每次接入设备新增一节，按如下格式记录。不要粘贴完整 MAC、序列号、USB 拓扑、用户应用路径或原始日志。

## 2026-08-28 · 实体输入观察准备

- **桌面端观察入口：** 新增 `--input-observe-only`。该模式不加载本地配置，不启动蓝牙配置通道，不发送 S3C 或 S3R；配置确认与 Host Action 均在应用层忽略。界面仅展示 USB、蓝牙和总标准 HID 报告计数，不保留报告 ID、键值、原始报文或时序。
- **现场状态：** 新包已在测试板的 USB HID 正常模式下启动，连接状态为 USB 已连接。观察计数初始化为零；本条未按下实体控件，不能以零计数替代输入验证。
- **输入观察边界：** 固件标准写入未覆盖 NVS，状态接口也不导出当前实体映射。因此未确定可安全短按的既有控件前，不触发可能产生未知键盘效果的物理输入。USB-only、BLE-only、双连接的单发和旧动作回归均保持 UNKNOWN。
- **软件证据：** EasyMacBord Swift 测试 105/105 通过，arm64 发布包签名与打包通过；Maker `7619bd1` 宿主测试 60/60 通过。Maker 源码、ESP-IDF 版本和候选镜像未因本条改动。
- **下一步：** 由操作者指定一个确认无副作用的既有实体控件后，依次执行 USB-only、BLE-only 与双连接观察。观察结果只记录报告来源、计数变化与单发结论。

## 模板

- **日期与应用版本：**
- **设备协议身份：** USB `VID/PID` 与蓝牙服务/特征是否匹配；PASS / FAIL / UNKNOWN
- **设备固件能力：**
- **连接方式：** USB-only / 仅蓝牙配置通道 / 双连接
- **配置确认：** bytes、crc16 是否匹配；结果 PASS / FAIL / UNKNOWN
- **可观察目标：** 计算器（Host Action）、TextEdit（固定文本、快捷键、复制/粘贴）
- **真机项目：** HW-01 至 HW-07 的人工观察和结果；未执行逐项标记 UNKNOWN
- **失败层：** 连接 / 写入 / 保存确认 / HID 消息 / 映射 / 权限 / 执行
- **未验证项：**

## 2026-08-27 · 固件写入一期标准写入

- **目标板识别：** 专用测试板在写入前重新识别为 ESP32-S3，Flash 容量与 16 MB 候选配置一致；目标身份在写入前再次比对一致。记录不包含端口或设备唯一标识。
- **写入结果：** 标准三段写入完成，bootloader、分区表和 factory app 的传输校验均通过。未运行整片 `erase_flash`；标准写入仅处理三段对应的目标扇区，NVS 及其他分区未作为写入目标。
- **启动验证：** 在确认整板真实断电并恢复供电后，USB HID 身份重新枚举，结果 PASS。下载镜像保持既有 `0x10`、`0x11`、`0x13` 报告声明；本次只读实机验证了 `0x13` 请求与 `0x11 / kind 0x04` 状态响应，未发送 `0x10`。
- **状态验收：** 烧录后仅发送一次 S3R 请求，结果 PASS：请求 ID、分片顺序、声明总长度和 CRC 校验通过，状态文档为 `ai_keyboard.config_status.v1`，`semantic_actions` 与两项 PTT 字段均存在。未导出状态 JSON、配置内容或字段值。
- **软件基线：** 烧录后复跑 EasyMacBord Swift 测试 102/102、Maker 宿主测试 60/60，均通过；ESP-IDF 5.5.5 默认构建保持此前已通过的候选基线。
- **边界：** 未发送 S3C、未验证保存确认、未发 Host Action，未修改 GPIO、分区、VID/PID、HID/GATT 合同或 Maker 生产源码。

## 2026-08-27 · 固件写入一期现场门禁

- **写入候选：** Maker 固定为 `7619bd1`，ESP-IDF 为 5.5.5，目标为 ESP32-S3。既有候选产物、16 MB Flash 配置和标准三段写入区域均已复核；NVS 不属于标准写入范围。
- **目标板识别：** 重新扫描下载设备时未发现可连接的下载接口，因此没有取得专用测试板的芯片、Flash 容量或硬件身份的本轮证据。
- **写入结果：** 未执行。没有进入下载模式、没有写入、没有擦除、没有复位，也没有把当前正常 HID 设备当作烧录目标。
- **下一步：** 接入可清空的专用测试板后，重新执行只读识别；只有目标为 ESP32-S3 且与本轮确认对象一致时，才进入标准三段写入。

## 2026-08-27 · 固件适配调试一期状态门禁复验

- **日期与应用版本：** 2026-08-27；EasyMacBord `--status-only` 状态门禁包。Maker 软件基线为 `7619bd1`，桌面端为 `feature/t05-actions-scenes`。
- **设备协议身份：** USB HID 已连接；本次不读取 BLE 配置服务、特征、加密或绑定状态，均为 UNKNOWN。
- **主机侧修复：** 保持 S3R v1 的 16 字节正文不变，macOS 提交缓冲区使用带 `0x13` 前缀的 17 字节 Feature Report；状态请求会先确认 HID 设备已打开，并在当前应用进程内只读检查 HID 监听授权。根据本次回包现象，接收端增加了对 macOS 可能保留输入 Report ID 的归一化处理，不改变固件的 63 字节应用命令正文或既有路由规则。
- **状态读取：** 在只读模式下，配置同步入口保持禁用，蓝牙配置通道未启动。完成当前包的授权后，仅发送一次 S3R 请求并成功完成状态重组，结果 PASS：请求 ID 一致、分片顺序连续、声明总长度一致、CRC 校验通过，状态文档为 `ai_keyboard.config_status.v1`，`semantic_actions` 与两项 PTT 字段均存在。未记录状态 JSON、配置内容或字段值。
- **配置确认：** 未发送 `0x10 / S3C`，未读取或验证 `0x11 / kind 0x03`；bytes、CRC16 和保存结果均为 UNKNOWN。
- **固件写入：** 专用测试板未接入，未执行设备识别、下载模式、擦除、烧录、复位或正常启动观察。
- **软件基线：** EasyMacBord Swift 测试 102/102、arm64 release 打包通过。DMG 根目录包含可选择的 `EasyMacBord.app`，挂载后签名校验通过；本地发布包已使用稳定的应用标识，仍需对该新包完成一次输入监控授权。Maker 宿主测试 60/60、ESP-IDF 5.5.5 默认构建通过。
- **失败层：** 本次状态读取无失败。先前的 macOS HID 授权和主机输入报告归一化问题已通过重新授权及最小主机侧兼容修复排除；当前没有证据表明 Maker 的状态分片或固件状态合同存在偏差。
- **未验证项：** BLE 配置服务、特征、加密和绑定状态，以及 USB-only、BLE-only、双连接实体输入单发与既有动作回归均为 UNKNOWN。保存确认和全部配置写入继续不在本轮范围内。

## 2026-08-27 · 固件适配调试一期（只读）

- **日期与应用版本：** 2026-08-27；EasyMacBord 只读状态包。Maker 软件基线为 `7619bd1`，桌面端为 `feature/t05-actions-scenes`。
- **设备协议身份：** EasyInput V2 / ESP32-S3；USB HID 身份和 `0x10`、`0x11`、`0x13` 报告声明匹配，PASS。BLE HID 可见，PASS；自定义配置服务/特征及加密、绑定状态未读取，UNKNOWN。正常模式无 CDC 串口不判失败。
- **设备固件能力：** 只读检查确认 Maker 源码具备 Status HID v1 描述符与接收处理链。实体设备未返回状态内容，故 firmware、capabilities、`semantic_actions`、两项 PTT 字段均为 UNKNOWN。
- **连接方式：** 只完成 USB HID 状态读取路径与 BLE HID 可见性检查；未建立或操作蓝牙配置通道。
- **配置确认：** 未发送 `0x10 / S3C`，未读取或验证 `0x11 / kind 0x03`；bytes、CRC16 和保存结果均为 UNKNOWN。
- **状态读取：** `--status-only` 模式下同步入口已禁用、蓝牙配置未启动。一次 `0x13 / S3R v1` 请求在 macOS USB HID Feature Report 提交阶段受阻，未收到 `0x11 / kind 0x04`。未记录 request ID、原始报文、状态 JSON 或配置内容。
- **可观察目标：** 未执行实体输入或 Host Action；既有动作不以其他应用的历史观察替代。
- **真机项目：** USB-only、BLE-only、双连接单发和旧动作回归均为 UNKNOWN。
- **失败层：** USB HID Feature Report 提交。未进入状态分片、BLE 特征、输入路由或 macOS 动作权限层。
- **未验证项：** S3R 回包的长度/顺序/CRC/schema、语义动作/PTT 能力、BLE 配置服务与安全状态、实体输入矩阵、保存确认，以及全部配置写入。

## 2026-08-26 · 设备身份预检

- **日期与应用版本：** 2026-08-26；`0.1.0-beta.1`。
- **设备协议身份：** USB `VID 0x303A / PID 0x1006`、产品名 `EasyInput AI`、厂商 `AIOTWAN` 均与联调约束一致，结果 PASS。系统同时发现同名 `Bluetooth Low Energy` HID 设备，最大输入报告为 64 字节；自定义 EasyInput V2 配置服务和特征尚未读取，结果 UNKNOWN。
- **设备固件能力：** 仅完成系统枚举观察；尚未向设备写入配置，未读取 `0x04` 状态响应。
- **连接方式：** USB 与 BLE HID 均已被系统枚举；未开始事件通道或配置通道联调。
- **配置确认：** 未执行配置写入，`0x11 / kind 0x03` 的保存状态、长度和 CRC16 均为 UNKNOWN。
- **可观察目标：** 尚未启动人工动作验证；后续固定使用“计算器”和 TextEdit。
- **真机项目：** HW-01 至 HW-07 均为 UNKNOWN，未以枚举结果替代人工观察。
- **失败层：** 无。本次仅做只读身份预检。
- **未验证项：** BLE 自定义配置服务与特征、USB/BLE 事件接收、单发行为、保存确认、Host Action 与既有输入回归。

## 2026-08-27 · T05 状态与扩展能力边界（未执行）

- **日期与应用版本：** 2026-08-27；T05 开发中，未产生新的实体设备观察或验收结论。
- **设备协议身份：** 未新增设备读取或连接观察。本条只记录计划复用上游 Status HID v1：USB Feature Report `0x13 / S3R v1` 请求、`0x11 / kind 0x04` 分片响应；实际 request ID、分片、长度和 CRC 结果均为 UNKNOWN。
- **设备固件能力：** `capabilities.semantic_actions` 尚未从实体设备状态 JSON 读取，结果 UNKNOWN。灯光和音乐律动没有当前 Mac 端可消费的设备控制合同，结果 UNKNOWN / 不可用。
- **连接方式：** 未执行 USB-only、仅 BLE 或双连接的 T05 状态读取。
- **配置确认：** 本条未写入配置；`0x03` 保存确认及 bytes、CRC16 均保持 UNKNOWN。
- **可观察目标：** 未执行前台应用自动切换后的同步、状态读取、语义/PTT 门控、清空废纸篓本机确认或 GitHub Release 列表检查与外部链接观察。
- **真机项目：** HW-01 至 HW-07 全部保持 UNKNOWN。
- **失败层：** 不适用；本条不记录失败，只保留未执行边界。
- **未验证项：** Status HID 请求/响应、语义/PTT 动作能力、前台应用自动切换后的自动同步、保存确认、USB/BLE 路由、灯光/律动新合同和全部实体输入回归。
