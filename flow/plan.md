# 实施计划

- [x] P01：建立独立 Git 工作目录、SwiftPM 应用骨架和文档目录。
- [x] P02：冻结 EasyInput 消费端协议与纯逻辑测试向量。
- [x] P03：实现状态总览、菜单栏、配置档编辑和权限页面。
- [x] P04：实现 USB/蓝牙配置通道传输适配与 USB 优先路由代码。
- [x] P05：在完整 Xcode 环境运行全量测试并修正编译问题。
- [ ] P06：连接兼容设备，完成配置确认和七项真机矩阵。
- [x] P07：打包 arm64 `.app`、ad-hoc 签名和 DMG，形成发布记录。
- [x] P08：在终端完成 GitHub 登录后与 `Jiangshuo1109/EasyMacBord` 同步。
- [x] P09：完成四页导航、三栏配置编辑器、同步历史、未知设备信息和动作测试边界的 UI 改造；补齐对应单元测试。
- [ ] P10：在 `1280 x 800` 与 `1120 x 720` 完成 T02/T03 及 T04 页面截图验收；截图只证明桌面端布局与状态。
- [ ] P11：在 T04 UI、M-01 至 M-08 和 HW-01 至 HW-07 均有实际结论且 GitHub CI 通过后，完成 `v0.1.0-beta.1` 分支审查、ad-hoc DMG、checksum、标签与发布记录。
- [ ] P12：完成 T03 可配置工具与本机应用目录的 M-01 至 M-08 人工验收；应用扫描、动作库登记、直接系统工具、快捷指令模板和单元测试已经完成，不修改设备消费者合同。
- [x] P13：完成 T04 桌面体验与动作中心：主窗口恢复、独立动作库、应用图标显示、输入预设与组合键录制、宏键盘映射、设置/关于和权限模型；逻辑测试、arm64 Release 构建和 ad-hoc 打包均已通过。
- [ ] P14：接入兼容设备后，按 HW-01 至 HW-07 完成 USB/BLE 配置确认、输入单发和回归矩阵；未执行项保持 `UNKNOWN`。
- [x] P15：完成 T05 动作与场景的代码实现与逻辑验证：前台应用自动切换规则、Status HID v1 读取、语义动作/PTT 门控、750 ms 自动同步、内建组合键预设、喝水提醒、清洁屏幕遮罩、废纸篓本机确认和 GitHub Release 网络检查；不改设备合同。真实系统和设备验收另计。
- [x] P16：在兼容设备上完成 USB Status HID v1 只读验证：`0x13` 请求与 `0x04` 分片状态响应的 request ID、长度、CRC、schema 与 `semantic_actions` 通过；灯光与音乐律动保持 `UNKNOWN`。BLE 配置、保存确认和实体输入仍由 P06/P14 验收。

每项完成必须在 `flow/进展.md` 写入证据路径、实际命令、结果和仍未验证项。构建通过不等于配置确认；配置确认不等于真机功能通过。
