# 协作约定

## 提交前

1. 阅读 `flow/plan.md`、本次相关协议与测试文档。
2. 先为协议、映射或路由行为补测试，再修改实现。
3. 运行全部可用测试；工具链缺失时如实记录原因，不把未执行写为通过。
4. 更新 `flow/进展.md` 和必要的决策记录。

## 禁止提交

- `.build/`、DerivedData、`.app`、DMG、日志和诊断包。
- 设备完整 MAC、序列号、USB 拓扑、用户选定应用路径、UUID、用户文本和权限截图。
- 任何 Wi-Fi 凭据、API Key、令牌或本机绝对路径。

## 协议变更

设备协议字段、HID 报告 ID、UUID 格式、触发时机或路由策略的任何变化，都必须先更新 `docs/技术/架构与协议.md`、补充测试向量并写入 `flow/decisions.md`。本仓库不直接修改 Maker 固件；需要固件变更时另建上游任务。
